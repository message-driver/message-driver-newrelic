require "spec_helper"

RSpec.describe MessageDriver::NewRelic do
  it "has a version number" do
    expect(MessageDriver::NewRelic::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
